import React from "react";

interface Props {};

const Home = (props: Props) => {
    return (
        <p className='text-red-700'>
            Hello world
        </p>
    );
};

export default Home;
